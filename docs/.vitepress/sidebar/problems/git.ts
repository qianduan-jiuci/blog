import { DefaultTheme } from 'vitepress'

const gitSide = {
  '/problems/git/': [
    {
      link: '/problems/git/index',
    },
    {
      text: 'refusing to merge unrelated histories',
      link: '/problems/git/refusing-to-merge-unrelated-histories',
    },
  ],
} as DefaultTheme.SidebarItem

export default gitSide
